<div class="row">
	<div class="col-md-10 col-sm-9">
		<h2>Posts</h2>
	</div>
	<div class="col-md-2 col-sm-3">
		<h2>
		<%= link_to new_admin_post_path, :class => 'btn btn-default btn-lg btn-block' do %>
			<i class="fa fa-plus"></i> New Post</a>
		<% end %>
		</h2>
	</div>
</div>

<div class="table-responsive">
	<table class="table table-hover">
	  <thead>
	    <tr>
	      <th>Title</th>
	      <th>Link</th>
	      <th>Author</th>
	      <th>Published date</th>
	      <th>Actions</th>
	    </tr>
	  </thead>

	  <tbody>
	    <% @posts.each do |post| %>
	      <tr>
	        <td><%= link_to post.title, post %></td>
	        <% if post.link %>
		      <td><span class="glyphicon glyphicon-link"></span></td>
	        <% else %>
	        <td>&nbsp;</td>
	        <% end %>
					<td><%= post.user_id ? User.find(post.user_id).first_name : nil %></td>
					<td><%= post.created_at.in_time_zone %></td>
					<td>
						<%= link_to edit_admin_post_path(post), :class => 'btn btn-default btn-sm' do %>
							<i class="fa fa-edit fa-lg"></i> Edit
						<% end %>
						<%= link_to admin_post_url(post), method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger btn-sm' do %>
							<i class="fa fa-trash-o fa-lg"></i> Delete
						<% end %>
					</td>
	      </tr>
	    <% end %>
	  </tbody>
	</table>
</div>

<!DOCTYPE html>
<html>
<head>
  <title>WesHargrove.com</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<% if current_user %>
		<header class="container">
			<p class="text-right">Logged in as <%= current_user.first_name %>.
			<%= link_to '(Not ' + current_user.first_name + '?)', logout_path, method: :delete %></p>
	<% else %>
		<header class="container">
			<p class="text-right"><%= link_to 'Log in', login_path %></p>
	<% end %>
			<h1 class="text-left"><%= link_to 'WesHargrove.com', root_path %></h1>
		</header>

	<div class="container">
		<%= yield %>
	</div>

	<footer>
		<p class="text-center text-muted"><small>Powered by <%= link_to 'Vino', 'https://github.com/whargrove/Vino' %>. Hosted on <%= link_to 'Digital Ocean', 'https://www.digitalocean.com/?refcode=f391b77be531' %>.</small></p>
	</footer>

</body>
</html>

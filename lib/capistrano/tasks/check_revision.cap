desc "Make sure local git is in sync with remote"
task :check_revision do
	on roles(:web) do
		unless `git rev-parse HEAD` == `git rev-parse origin/master`
			error "HEAD is not the same as origin/master"
			error "Run `git push` from local master to sync changes" 
			exit
		end
	end
end
